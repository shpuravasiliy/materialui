import React from 'react';
import {Grid, Typography} from "@mui/material";

const Database = () => {
    return (
        <Grid item xs={8}>
            <Typography variant={'h2'}>This is Database page.</Typography>
        </Grid>
    );
};

export default Database;