import React from 'react';
import {Grid, Typography} from "@mui/material";

const Functions = () => {
    return (
        <Grid item xs={8}>
            <Typography variant={'h2'}>This is Functions page.</Typography>
        </Grid>
    );
};

export default Functions;