import React from 'react';
import {Grid} from "@mui/material";

const Functions = () => {
    return (
        <Grid item xs={8}>
            <h1>This is Functions page.</h1>
        </Grid>
    );
};

export default Functions;